# Desenvolvimento local (aponta para o backend Fastify)
VITE_API_URL=http://localhost:3333/api

# Produção na Vercel — deixe em branco ou remova esta variável
# O fallback automático já aponta para /_/backend/api (mesmo domínio)
